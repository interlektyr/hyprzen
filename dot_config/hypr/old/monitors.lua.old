------------------
---- MONITORS ----
------------------

local INTERNAL = "desc:AU Optronics 0xC199"
--local EXTERNAL = "desc:Samsung Electric Company LS24D60xU HNAXC00823"

-- See https://wiki.hypr.land/Configuring/Basics/Monitors/
--Samsung Electric Company LS24D60xU HNAXC00823 DP-1
--AU Optronics 0xC199 eDP-1
hl.monitor({
	output = "",
	mode = "preferred",
	position = "auto",
	scale = 1.07,
})

hl.monitor({
	output = INTERNAL,
	mode = "preferred",
	position = "auto",
	scale = 1.25,
})

--hl.on("monitor.removed", function()
--	hl.monitor({ output = INTERNAL, disabled = false })
--end)

--local function external_count()
--	local n = 0

--	for _, m in ipairs(hl.get_monitors() or {}) do
--		if not (m.name and m.name:sub(1, 3) == "eDP") then
--			n = n + 1
--		end
--	end

--	return n
--end

--local function apply()
--	if external_count() >= 1 then
--		hl.monitor({ output = INTERNAL, disabled = true })
--	else
--		hl.monitor({
--			output = INTERNAL,
--			mode = "preferred",
--			position = "0x0",
--			scale = 1.25,
--		})
--	end
--end

--hl.on("monitor.added", apply)

--hl.on("monitor.removed", apply)

--hl.on("config.reloaded", apply)
